# ADC 健康监测和 LED 状态指示：代码阅读说明

这次继续使用原来的 5 个业务任务，没有增加业务任务或引入状态机框架。启动任务完成初始化后删除自身；FreeRTOS 的空闲任务、定时器服务任务不计入这 5 个业务任务。

## 1. 建议按这个顺序阅读

| 文件（位于 `18FreeRTOS项目`） | 主要看什么 |
|---|---|
| `BSP/start_task.c` | 创建两级队列、互斥量和 5 个任务 |
| `BSP/adc.c` | TIM3 → ADC1 → DMA1 Channel1 → 通知健康任务 |
| `BSP/user_temp.c` | 求平均、换算温度/电压、判断告警 |
| `BSP/user_temp.h` | 修改参考电压、温度参数、告警阈值 |
| `BSP/user_ledtask.c` | `Led_SelectMode` 选灯效，`Led_ShowPhase` 控制亮灭节奏 |
| `BSP/user_task2.c`、`BSP/onenet_ota.c` | 在真实 OTA 处理位置更新 LED 状态 |

## 2. ADC 数据怎样走

TIM3 每 10 ms 触发一次 ADC，依次转换内部温度通道 16 和 Vrefint 通道 17。TIM3 只产生内部触发，不占外部引脚，也没有定时器中断。

DMA 环形缓冲区分成两半，每半块保存 16 组数据（32 个半字），所以正常情况下每 160 ms 产生一次半满/全满中断。中断将已完成的半块复制到快照缓冲区，再通知健康任务。任务读取快照时用很短的临界区保护，不直接读取 DMA 正在写的区域。中断中不做浮点计算或串口打印。

这是健康监测用的“取最新块”设计：如果任务处理延迟，旧块可以被新块替换，不保存全部采样历史；复制时发生半块切换会丢弃该次快照。DMA 传输错误或超过 1 秒没有通知会显示采集故障。

健康任务分别对 16 个温度原始值和 16 个参考电压原始值取平均，然后计算：

```text
VDDA(mV) = Vrefint(mV) × 4095 / ADC参考通道均值
温度传感器电压(mV) = ADC温度通道均值 × VDDA / 4095
温度(℃) = (1430 - 温度传感器电压) / 4.3 + 25
```

前提是板上 VREF+ 与 VDDA 相连；若单独给 VREF+ 供电，反推结果是 VREF+ 电压，不能称为 VDDA。测到的也不是 USB 5V 或电池电压。

Vrefint 默认 1200 mV，25℃温度传感器电压默认 1430 mV，斜率默认 4.3 mV/℃，都来自 [STM32F103xC/xD/xE 数据手册](https://www.st.com/resource/en/datasheet/stm32f103ze.pdf) 的典型值。芯片间存在偏差，供电补偿不等于绝对精度校准；内部传感器测的是芯片温度。需要更准时，修改 `user_temp.h` 中三个参数进行板级校准。

`g_health` 保存最新结果，可在 Keil Watch 中观察。其他任务使用 `Health_GetLatest(&sample)`，可以获得一致的快照和过期判断。串口每秒输出一次，例如：

```text
Health: 32.5 C, 3298 mV, hot=0 low=0
```

## 3. 怎么调整告警

直接改 `user_temp.h`：

| 告警 | 进入条件 | 解除条件 |
|---|---|---|
| 过温 | ≥ 75℃ | ≤ 70℃ |
| 低压 | ≤ 3000 mV | ≥ 3100 mV |

例如已经过温报警，降到 73℃时仍保持报警，降到 70℃才解除，这就是“滞回”。滤波只用了算术平均，没有叠加卡尔曼或其他滤波器。

这些阈值用于演示检测与告警。本版没有因低压/过温暂停 OTA、禁止 Flash 擦写，也没有新增健康数据的 OneNET 遥测接口。

## 4. LED 怎样响应真实状态

只有 LED 任务负责运行期间的亮灭。其他任务调用以下三个接口：

```c
Led_SetNetwork(1);                 /* 最近一次联网成功 */
Led_SetHealth(hot, low, adc_fault); /* 健康任务提交告警 */
Led_SetOtaState(LED_OTA_FLASH);     /* 即将执行 Flash 操作 */
```

接口只在状态发生变化时发送任务通知。LED 任务等待通知或当前亮灭阶段超时，不使用忙等。`Led_SelectMode()` 从上往下判断优先级：

```text
ADC/OTA故障 > 低压 > 过温 > Flash > 校验 > 成功 > 下载 > 离线 > 正常
```

| 状态 | 灯效 |
|---|---|
| 正常 | 亮 100 ms，灭 1900 ms |
| 网络通信失败/尚未连接 | 每 2 秒双闪 |
| OTA 下载 | 亮灭各 200 ms |
| Flash 擦除/写入 | 常亮 |
| CRC 校验 | 每秒双闪 |
| OTA 成功 | 亮灭各 100 ms，600 ms 后重启 |
| ADC/OTA 故障 | 三闪后停 1500 ms |
| 低压 | 亮灭各 250 ms |
| 过温 | 亮灭各 500 ms |

LED 默认 PB5 低电平亮。如果板上相反，修改 `led.h` 的 `LED_ACTIVE_LOW` 为 0。Flash 写入、校验阶段可能很短，肉眼不一定看得清；代码没有为了延长灯效而拖慢每块写入。

网络状态取自现有入网、版本上报、任务查询的结果，不是持续检测的物理 Wi-Fi 状态。空闲时仍使用原来的 10 分钟查询间隔，断网可能等到下一次通信才发现。OTA 失败灯效保留到下一轮升级；健康告警解除后，显示会恢复到当前 OTA/网络状态。

## 5. 两级队列与任务通知

```text
CAN 分帧接收 / OneNET 收到一块固件
    → OTA_Queue
    → Protocol_Task（解释消息，生成擦除/写入/校验作业）
    → Job_Queue
    → Flash_Write_Task（实际操作 Flash）
    → CAN ACK，或任务通知 OneNET 操作结果
```

保留了原有队列和 ACK/任务通知机制。恢复了 `CAN_Tp.c` 中被注释的单帧数据入队、多帧接收完成入队；添加 DLC/帧类型检查和 `CAN_QueueDropCount` 丢包计数。CAN 队列满时中断不阻塞、不发送成功 ACK，由现有 PC 脚本超时重发。现有脚本每块 256 字节固件，加 2 字节序号后共 258 字节，能放入 512 字节接收缓冲区。

任务优先级现在明确为：协议 5、OneNET 4、LED 3、健康 2、Flash 1，`configMAX_PRIORITIES` 改为 6。LED 操作很短并立即阻塞；健康任务等待 DMA 通知；OneNET 等待 Flash 通知，避免高优先级任务空转饿死 Flash 任务。

CAN 中断优先级改为 5，DMA 中断为 6，与当前 FreeRTOS 系统调用中断阈值兼容。

队列实现的是软件职责解耦，不保证 Flash 忙时 CPU 完全不停顿。STM32F103ZE 的片内 Flash 擦写仍可能阻塞取指，不能仅凭采用队列就在简历上声称通信绝对无阻塞。建议写“将耗时 Flash 操作移出接收与协议处理路径”。

## 6. 如何验证

1. 打开原来的 `18FreeRTOS项目/Project/STM32.uvprojx` 编译，新增逻辑都在原工程已包含的源文件中。
2. 烧录后看串口温度、电压和 `g_health`；用万用表测量 VDDA，核对补偿结果，必要时标定。
3. 想演示告警时，可临时将过温进入/解除阈值一起调到低于当前测量值，再恢复；也可在调试器中停止 TIM3，验证超时故障。避免通过给芯片超额供电制造告警。
4. 断网、重新联网、进行一次正常 OTA 和 CRC 错误 OTA，观察相应灯效。成功升级仍有原来的 ACK，重启前延时由 100 ms 改成 600 ms。
5. PC 逻辑测试执行 `powershell -File tests/health_led/run.ps1`。脚本默认使用本机 GCC，也可以通过 `-Compiler` 指定 GCC 路径。测试文件不加入 Keil 工程。

PC 测试直接调用真实业务源码，覆盖补偿/均值、滞回、零值和满量程、过期数据、tick 回绕、LED 优先级和节奏、CAN 分帧/包序号/长度检查和队列满后的重试。它们不能替代真实 MCU 的 DMA 时序、中断延迟、温度校准、供电测量和 OTA 上板联调。
